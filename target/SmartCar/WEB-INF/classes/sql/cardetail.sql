CREATE DATABASE `smartcar`;

CREATE TABLE IF NOT EXISTS `carinfo`(
    `id` INT PRIMARY KEY AUTO_INCREMENT, 
    `carid` VARCHAR(20) PRIMARY KEY, `info` VARCHAR(400), 
    `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);